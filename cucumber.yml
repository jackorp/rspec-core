<%
exclusions = []
exclusions << ' --tags "not @no-jruby"' if RUBY_PLATFORM == 'java'
%>
default: --require features --strict --format progress --tags "not @wip<%= exclusions.join %>" features
wip:     --require features --tags "@wip:30" --wip features
